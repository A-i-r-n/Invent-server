- @page_title = t('shoppe.products.products')
= content_for :header do
  %p.buttons
    = link_to t('shoppe.localisations.localisations') , [:admin,@product, :localisations], :class => 'button'
    = link_to t('shoppe.products.variants') , [:admin,@product, :variants], :class => 'button'
    = link_to t('shoppe.products.stock_levels') , admin_stock_level_adjustments_path(:item_id => @product.id, :item_type => @product.class), :class => 'button', :rel => 'dialog', :data => {:dialog_width => 700, :dialog_behavior => 'stockLevelAdjustments'}
    = link_to t('shoppe.products.back_to_products') , [:admin,:products], :class => 'button'
  %h2.products= t('shoppe.products.products')
= render 'form'
