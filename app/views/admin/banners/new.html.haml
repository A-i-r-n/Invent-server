- @page_title = t('shoppe.products.products')

= content_for :header do
  %p.buttons= link_to t('shoppe.products.back_to_products') , [:admin,:banners], :class => 'button'
  %h2.products= t('shoppe.products.products')

= render 'form'
