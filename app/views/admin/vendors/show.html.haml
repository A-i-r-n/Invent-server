- @page_title = "商户"

= content_for :header do
  %p.buttons
    = link_to t('shoppe.edit'), [:edit,:admin, @vendor], :class => 'button'
    - if @vendor.accepted?
      -#= link_to t('shoppe.orders.despatch_note.despatch_note'), [:despatch_note,:admin, @vendor], :class => 'button', :rel => 'print'
    = link_to t('shoppe.orders.back_to_orders'), [:admin,:orders], :class => 'button grey'
  -#%h2.orders= t('shoppe.orders.order_no_html', order_number: @order.number)

#order

  -#- if @order.received?
  = render 'vendor_details'
  -#  = render 'status_bar'
  -#- else
  -#  %p.warning= t('shoppe.orders.in_progress_warning')

  -#= field_set_tag t('shoppe.orders.order_items'), :class => 'padded' do
  -#  = render 'order_items'

  -#- if @order.received?
  -#  = field_set_tag t('shoppe.orders.payments'), :class => 'padded orderPayments' do
  -#    = render 'payments_table'
  -#    = render 'payments_form'
